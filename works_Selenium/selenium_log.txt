11:36 2019/3/15
1、显示等待:直到元素出现才去操作，如果超时则报异常
WebDriverWait(driver, timeout, poll_frequency=0.5, ignored_exceptions=None)
——driver：WebDriver 的驱动程序(Ie, Firefox, Chrome 或远程)
——timeout：最长超时时间，默认以秒为单位
——poll_frequency：休眠时间的间隔（步长）时间，默认为 0.5 秒
——ignored_exceptions：超时后的异常信息，默认情况下抛 NoSuchElementException 异常
例：WebDriverWait(driver, 10).until(lambda driver: driver.findElement(By.Id("someId"))) ;
//每隔0.5秒扫描一次检查是否有id为someId的元素，10秒后没找到就不找了抛异常。

WebDriverWait()一般由 unit()或 until_not()方法配合使用
until(method, message=’ ’)
调用该方法提供的驱动程序作为一个参数，直到返回值不为 False。

2、隐式等待:隐式等待针对全局
优：设置一次即可。
缺：但是要等全部加载才行，有些load慢的就影响效率。