【自己用selenium webdriver，在飞燕0.5上，写过:
隐身登录，切换frame到登录box
find element登录框，sendkeys跳转，
到某个项目的产品中，方便测试。隐身模式登录（方便测试时清缓存）】

1、用隐身模式访问（ChromeOptions，add_argument('--incognito')）
2、get地址
3、切换到登录窗口的frame。switch_to.frame("alibaba-login-box")
print(driver.title)
4、通过id查找元素find_element_by_id，   .send_keys
5、find_element_by_link_text("下次再说").click()
5、通过xpath访问项目（Chrome检查，copy xpath）

常用
find_element_by_id()
find_element_by_name()
find_element_by_link_text()
find_element_by_xpath()
find_element_by_css_selector()

Selenium IDE：浏览器插件